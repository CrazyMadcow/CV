document.addEventListener("DOMContentLoaded",()=>{const y=document.getElementById("year");if(y)y.textContent=new Date().getFullYear();const links=[...document.querySelectorAll('.top-nav a[href^="#"]')];const sections=links.map(a=>document.querySelector(a.getAttribute("href"))).filter(Boolean);const onScroll=()=>{let current="";for(const section of sections){if(window.scrollY>=section.offsetTop-130)current=section.id}links.forEach(a=>a.classList.toggle("active",a.getAttribute("href")===`#${current}`))};document.addEventListener("scroll",onScroll,{passive:true});onScroll();});